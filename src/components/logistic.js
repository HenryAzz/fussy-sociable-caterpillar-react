import React from 'react'

import PropTypes from 'prop-types'

import './logistic.css'

const Logistic = (props) => {
    return (
      <section>
        <div>
            
        </div>
      </section>

    )}